ПОЧТА НА ЖИВОМ СЕРВЕРЕ — прогон «Первичная настройка сервера» № 10,
2026-09-24, 10:07…10:15 UTC. Снято С САМОГО СЕРВЕРА и с раннера:
из среды разработки ни spotik.shop, ни smtp.yandex.ru не видны вовсе.

ИТОГ ОДНОЙ СТРОКОЙ: ПИСЬМО НЕ УШЛО. Вся наша сторона цепочки прошла
целиком, отказ пришёл от Яндекса на шаге AUTH PLAIN.

── 1. перенос секретов в /srv/spotik/env ──
  SMTP_HOST: записан
  SMTP_PORT: записан
  SMTP_USER: записан
  SMTP_PASS: записан
  SMTP_FROM: записан
  SMTP_SECURE: записан            <- выведен из порта 465
  служба перезапущена: значения действительно изменились

── 2. окружение ЖИВОЙ службы (читается /proc/<pid>/environ) ──
  SMTP_HOST: задан
  SMTP_PORT: задан
  SMTP_USER: задан
  SMTP_PASS: задан
  SMTP_FROM: задан
  SMTP_SECURE: задан
  адрес в SMTP_FROM и SMTP_USER: совпадают
  порт и TLS: 465 с неявным TLS — как надо у Яндекс 360
  адреса smtp.yandex.ru: 2a02:6b8::19d, 77.88.21.158
  порт smtp.yandex.ru:465 с сервера открыт

  ⚠️ ПЕЧАТАЮТСЯ НЕ ЗНАЧЕНИЯ, А ПРИГОВОР: SMTP_* — секреты
  репозитория, GitHub маскирует их в журнале звёздочками,
  и диагностика на печати значений исчезла бы вместе с ними.
  Сравнение делает сам сервер.

── 3. отправка НАСТОЯЩИМ путём (Р-99) ──
  Код входа запрошен живой формой https://spotik.shop/admin/login/
  обычным multipart-POST со всеми её скрытыми полями: та же цепочка
  adminAskCode → poprositKod → otpravit → nodemailer → Яндекс, какой
  письма идут на бою. Ни отдельного скрипта, ни второй двери.

  кому: k…@mail.ru
  строк входа по этому адресу за час: 0, предел таблицы: 0
  страница входа ответила 200 на отправку формы
  код входа выдан, строка login_code № 2

── 4. что записала служба (единственное доказательство) ──
  2026-09-24T10:15:29.145Z ERR  письмо не ушло
      to="k…@mail.ru" subject="Spotik Shop — код входа в админку"
      ot="***" uzel="***:*** tls"
      vid="EAUTH" kod=535 shag="AUTH PLAIN"
      otvet="535 5.7.8 Error: authentication failed:
             Invalid user or password! 1790244929-TFaevREhsa60"

  ⚠️ ot= и uzel= вышли звёздочками не по ошибке: SMTP_FROM, HOST
  и PORT — секреты репозитория, и раннер их маскирует. Значение
  этих полей проверено ОТДЕЛЬНО, на сервере, и приговор — в п. 2.

  ИТОГ ПРОГОНА: Яндекс НЕ ПРИНЯЛ ЛОГИН И ПАРОЛЬ. Три причины дают
  один и тот же 535, и журнал их НЕ различает — различает только
  кабинет: (1) в SMTP_PASS не пароль приложения из id.yandex.ru,
  а пароль от аккаунта; (2) в SMTP_USER не полный адрес ящика;
  (3) в Яндекс 360 у ящика выключен доступ по протоколам.

── 5. что этим доказано и что нет ──
  ДОКАЗАНО: секреты доехали до окружения живой службы; служба
  перезапущена; узел Яндекса резолвится и порт с сервера открыт;
  неявный TLS включён; отправитель совпадает с ящиком; форма входа
  работает; nodemailer дошёл до разговора и получил ответ.
  НЕ ДОКАЗАНО и доказано быть не может отсюда: какая из трёх причин
  535 действует. Это видно только в кабинете Яндекса.

  ⚠️ ПОБОЧНОЕ СЛЕДСТВИЕ: войти на сайт сейчас нельзя никак. Запасной
  путь «письмо целиком в журнал» включается ТОЛЬКО при пустом
  SMTP_HOST, а он теперь не пуст.
