ТЕЛЕГРАМ НА ЖИВОМ СЕРВЕРЕ — прогон «Первичная настройка сервера» № 9,
2026-09-23, 18:55…18:57 UTC. Снято С САМОГО СЕРВЕРА, а не с раннера
и не из среды разработки: отсюда api.telegram.org недоступен вовсе.

── окружение ЖИВОЙ службы (читается /proc/<pid>/environ) ──
  TELEGRAM_BOT_TOKEN: задан
  TELEGRAM_CHAT_ID: задан
  ROBOKASSA_LOGIN: задан
  ROBOKASSA_PASS1: задан
  ROBOKASSA_PASS2: задан
  ROBOKASSA_TEST_PASS1: задан
  ROBOKASSA_TEST_PASS2: задан
  ROBOKASSA_TEST: 1 (1 — тестовый режим, 0 — боевой)
  ROBOKASSA_ALGO: md5

── доступность api.telegram.org с сервера ──
  AAAA api.telegram.org: 2001:67c:4e8:f004::9
  telegram по IPv6: HTTP 302 через 2001:67c:4e8:f004::9
  telegram по IPv4: HTTP 000 через            <- curl: (28) таймаут 8002 мс
  telegram по IPv4: не отвечает — так и ожидается
  бот отвечает: HTTP 200 через 2001:67c:4e8:f004::9

То есть предпосылка постановки подтверждена ПРЯМЫМ ЗАМЕРОМ: IPv4
до Telegram с этого сервера мёртв, IPv6 работает.

── сквозная отправка ЧЕРЕЗ КОД ПРИЛОЖЕНИЯ ──
  строка очереди № 1 поставлена
  УВЕДОМЛЕНИЕ УШЛО в 18:56:41 UTC, попыток: 0
  ── что записала служба ──
  2026-09-23T18:56:41.464Z INFO отложенное уведомление ушло
      vid="proverka" popytok=1 adres="2001:67c:4e8:f004::9 (IPv6)"

Строка легла в notify_outbox в 18:56:04, будильник службы забрал её
и отправил в 18:56:41 — 37 секунд, ПЕРВАЯ попытка (popytok остался 0:
он растёт только на неудаче). Обхода на IPv4 не случилось ни разу.

⚠️ ОТПРАВЛЯЛ НЕ `curl`, А САМА СЛУЖБА. Curl доказал бы достижимость
Telegram с сервера — и только. Здесь работает тот же код, который
зовут оплата и закрытие заказа (lib/server/telegram.ts, family: 6,
autoSelectFamily: false), и адрес соединения в журнал пишет он же.
«Ушло по IPv6» — наблюдение службы, а не наш вывод (закон 39).

⚠️ В ЖУРНАЛЕ РЯДОМ ЕСТЬ ОДНА ЧУЖАЯ СТРОКА, и она НЕ дефект:
  18:54:58 ERR уведомление Робокассы не принято why="net_poley"
Это живая проверка выкладки № 87 стучится в /api/pay/result/ пустым
POST и ждёт четырёхсотого: дверь обязана отказывать без подписи.

── чего отсюда не видно ──
Самого чата. Доказательство — HTTP 200 от sendMessage, а Bot API
отвечает двумястами только на ДОСТАВЛЕННОЕ сообщение: не добавлен
бот в чат или неверный идентификатор — это 400 или 403, и строка
осталась бы неотправленной с причиной в poslednyaya_oshibka.
Сообщение в чате помечено временем UTC и словами «тестовое
уведомление прогона настройки сервера».
