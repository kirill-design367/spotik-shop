/**
 * Частоты самообновления очереди.
 *
 * ⚠️ ЛЕЖАТ ОТДЕЛЬНО ОТ ДЕЙСТВИЙ, И ЭТО НЕ ВКУС. Файл действий помечен
 * `'use server'`, а из такого файла разрешено вывозить ТОЛЬКО
 * асинхронные функции: массив оттуда роняет сборку целиком. Здесь же
 * он нужен обеим сторонам — и разметке очереди, и проверке в действии.
 *
 * Набор взят из админки Нейролавки: выкл, 10 с, 30 с, 1 мин, 5 мин.
 * Список ЗАКРЫТЫЙ — значение приходит из формы, а частота опроса
 * с чужим числом это либо страница, стучащаяся каждую миллисекунду,
 * либо очередь, которая не обновляется никогда.
 */
export const CHASTOTY_OCHEREDI = [0, 10, 30, 60, 300] as const;
