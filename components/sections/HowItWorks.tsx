import SectionHead from './SectionHead';

/**
 * БЛОК 3 — КАК ЭТО РАБОТАЕТ. ПРИЁМЫ Б + В.
 *
 * Это МЕЛКИЙ такт ритма между двумя крупными — блоками 2 и 4, которые идут
 * одним приёмом. Поэтому здесь всё другое: набор втрое мельче их рядов
 * и выключен влево, а не по центру.
 *
 * Б — один тезис по левому краю, справа воздух.
 * В — под ним пять шагов мелким набором в три колонки, прописными
 *     с крупным трекингом: так они читаются как техническая опись,
 *     а не как ещё пять заголовков.
 *
 * Развилка «новый аккаунт или продление» объяснена отдельно, тем же
 * приёмом в две колонки.
 */
const STEPS = [
  {
    t: 'Выбираете тариф и срок',
    d: 'Индивидуальный, на двоих или на троих. Срок — от месяца до года, цена фиксируется сразу.',
  },
  {
    t: 'Оплачиваете картой или через СБП',
    d: 'Обычная российская карта или перевод по СБП. Ни зарубежных карт, ни криптовалюты, ни посредников.',
  },
  {
    t: 'Переходите в Telegram-бот',
    d: 'После оплаты открывается ссылка на бота. Он же остаётся каналом связи на весь срок подписки.',
  },
  {
    t: 'Получаете логин и пароль от аккаунта',
    d: 'Данные приходят в бот. Если продлевали свой аккаунт — приходит подтверждение, что Premium уже активен.',
  },
  {
    t: 'Входите в Spotify и слушаете',
    d: 'Приложение, браузер, колонка, телевизор — куда угодно. Без VPN и без изменения настроек сети.',
  },
];

const FORK = [
  {
    n: 'Развилка А',
    t: 'Создаём новый аккаунт',
    d: 'Подходит, если Spotify вы ещё не слушали или готовы начать с чистого листа. Заводим аккаунт, подключаем Premium и передаём вам логин с паролем. Фонотеку и плейлисты собираете сами — они останутся вашими.',
  },
  {
    n: 'Развилка Б',
    t: 'Продлеваем существующий',
    d: 'Подходит, если у вас уже есть аккаунт с плейлистами, подписками и историей прослушиваний. Ничего не переносим и не пересоздаём — Premium подключается к тому аккаунту, которым вы пользуетесь.',
  },
];

export default function HowItWorks() {
  return (
    <section id="how" className="section">
      <div className="shell">
        <SectionHead num="03" kicker="Порядок" title="Как это работает" meta="5 шагов · 2 развилки" quiet />

        {/* Б: тезис слева, справа воздух. Мера задана в знаках, поэтому
            воздух остаётся и на 2560, где места сколько угодно. */}
        <p className="stmt stmt--how rv">
          Пять шагов от выбора тарифа до первого трека. <em>Ни одного, который делаете вслепую.</em>
        </p>

        {/* В: три колонки мелкого набора с крупным трекингом */}
        <div className="cols">
          {STEPS.map((s, i) => (
            <section key={s.t} className="col rv" style={{ ['--rv-d' as string]: `${i * 70}ms` }}>
              <p className="col__n tnum">{String(i + 1).padStart(2, '0')}</p>
              <h3 className="col__t">{s.t}</h3>
              <p className="col__d">{s.d}</p>
            </section>
          ))}
        </div>

        <div className="cols cols--two">
          {FORK.map((f, i) => (
            <section key={f.t} className="col rv" style={{ ['--rv-d' as string]: `${i * 70}ms` }}>
              <p className="col__n">{f.n}</p>
              <h3 className="col__t">{f.t}</h3>
              <p className="col__d">{f.d}</p>
            </section>
          ))}
        </div>
      </div>
    </section>
  );
}
